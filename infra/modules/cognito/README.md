# Cognito Module

Planned resources:

- Cognito User Pool
- Cognito App Client
- Password and registration policies
- Optional groups: `user`, `admin`, `auditor`

The backend validates Cognito JWTs when `AUTH_REQUIRED=true`.
