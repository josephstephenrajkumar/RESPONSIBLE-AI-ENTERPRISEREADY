# Secrets Module

Planned resources:

- Secrets Manager secret for `GROQ_API_KEY`
- Secrets Manager secret for Aurora credentials
- Optional Langfuse secrets
- Parameter Store values for non-secret configuration
