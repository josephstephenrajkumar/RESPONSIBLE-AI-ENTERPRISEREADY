# Aurora PostgreSQL Module

Planned resources:

- Aurora PostgreSQL cluster
- Serverless v2 or small provisioned cluster depending on environment
- Private subnet placement
- Security group allowing connections from ECS only
- Secrets Manager credential integration
- Backup retention and deletion protection settings per environment

Aurora stores users, audits, guardrail violations, policies, and runtime policy decisions.
