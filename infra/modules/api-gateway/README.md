# API Gateway Module

Planned resources:

- HTTP API using the default AWS HTTPS domain
- Route integration to the ECS-hosted AI Gateway
- CORS configuration for the CloudFront frontend
- Access logs

No custom domain, ACM certificate, or Route53 record is included in v1.
